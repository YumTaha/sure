#!/usr/bin/env bash
set -euo pipefail
if ! command -v gitleaks >/dev/null 2>&1; then
  echo "❌ gitleaks not installed — refusing to commit unscanned. Install per GITHUB.md, then retry." >&2
  exit 1
fi
gitleaks protect --staged --redact --verbose
